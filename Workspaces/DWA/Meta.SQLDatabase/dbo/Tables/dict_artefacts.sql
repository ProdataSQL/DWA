CREATE TABLE [dbo].[dict_artefacts] (
    [id]           VARCHAR (MAX) NULL,
    [display_name] VARCHAR (MAX) NULL,
    [description]  VARCHAR (MAX) NULL,
    [type]         VARCHAR (MAX) NULL,
    [workspace_id] VARCHAR (MAX) NULL,
    [folder_id]    VARCHAR (MAX) NULL
);


GO

